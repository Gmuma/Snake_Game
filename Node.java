public class Node {
    int x, y; // The coordinates of the segment
    Node next; // Reference to the next node

    // Constructor for a Node
    public Node(int x, int y) {
        this.x = x;
        this.y = y;
        this.next = null;
    }
}
